{
  "version": 2,
  "builds": [
    {
      "src": "server/dist/app.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install && npx prisma generate"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/dist/app.js"
    }
  ]
}